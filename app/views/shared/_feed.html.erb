<% if @feed_items.any? %>
    <ol class="microposts" style="padding-left: 0px;">
      <%= render partial: 'shared/feed_item', collection: @feed_items %>
    </ol>
    <%= will_paginate(@feed_items, :class=>"fenye") %>
<% end %>